apiVersion: apps/v1
kind: Deployment
metadata:
  name: financebroke-backend
  namespace: financebroke
  labels:
    app: financebroke
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: financebroke
      component: backend
  template:
    metadata:
      labels:
        app: financebroke
        component: backend
    spec:
      containers:
      - name: backend
        image: financebroke:latest
        ports:
        - containerPort: 8080
        env:
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: financebroke-secrets
              key: DB_PASSWORD
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: financebroke-secrets
              key: JWT_SECRET
        - name: TELEGRAM_BOT_TOKEN
          valueFrom:
            secretKeyRef:
              name: financebroke-secrets
              key: TELEGRAM_BOT_TOKEN
        - name: SMTP_USERNAME
          valueFrom:
            secretKeyRef:
              name: financebroke-secrets
              key: SMTP_USERNAME
        - name: SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: financebroke-secrets
              key: SMTP_PASSWORD
        envFrom:
        - configMapRef:
            name: financebroke-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/v1/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: financebroke-service
  namespace: financebroke
spec:
  selector:
    app: financebroke
    component: backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP